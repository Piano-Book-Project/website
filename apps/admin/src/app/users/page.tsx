'use client';
import React from 'react';

export default function UsersPage() {
  // 메타태그 설정
  // useEffect 및 setPageMeta, setDefaultMeta 관련 코드 모두 삭제

  return (
    <div style={{ color: '#fff', fontSize: 32, padding: 40 }}>사용자 관리 페이지 (개발 예정)</div>
  );
}
